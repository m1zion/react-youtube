import "./App.css";

function course4useState() {
    const age = 0;
    const increaseAge = () ={

    };
    return (
        <div className="course4useState">
            {age}
            <button onClick={increaseAge}>Increase Age</button>

        </div>
    );
}
export default course4useState;